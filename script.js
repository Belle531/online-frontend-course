const quizButton = document.querySelector('#interactive-quiz button');
if (quizButton) {
    quizButton.addEventListener('click', function() {
        alert('Basic quiz functionality will be implemented in a future version!');
    });
}
